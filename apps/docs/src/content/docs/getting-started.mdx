---
title: Quick start
description: Install Profanity Kit and detect your first complete word.
sidebar:
  order: 1
  badge: Start here
---

## Install

<PackageManagers pkg="profanity-kit" />

## Detect and filter

```ts
import { createDetector } from "profanity-kit";

const detector = createDetector();

detector.check("This contains shit"); // true
detector.isClean("This is fine"); // true
detector.filter("hide the shit"); // "hide the ****"
```

The root entry includes the reviewed English pack. Detectors are immutable;
create a new detector when configuration changes.

## Inspect matches

```ts
const [match] = detector.findAll("hide the shit");
match.value; // "shit"
match.start; // 9
match.end; // 13
match.languages; // ["en"]
```

See [matching semantics](/matching) for Unicode boundaries and offsets.
